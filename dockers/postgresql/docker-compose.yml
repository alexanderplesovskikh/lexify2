version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: postgres-api
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Lexify2525@@
      POSTGRES_DB: postgres
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
